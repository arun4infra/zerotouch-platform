apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
- cilium/01-cilium.yaml
- components-tenants

# In production, we ensure targetRevision is main (or specific tag)
patches:
- target:
    kind: Application
    group: argoproj.io
  patch: |
    - op: replace
      path: /spec/source/targetRevision
      value: main