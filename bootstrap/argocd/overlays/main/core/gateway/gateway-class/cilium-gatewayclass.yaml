apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: cilium
  labels:
    app.kubernetes.io/name: cilium-gatewayclass
    app.kubernetes.io/component: gateway-controller
    app.kubernetes.io/part-of: zerotouch-platform
spec:
  controllerName: io.cilium/gateway-controller
  # Note: parametersRef removed due to Cilium v1.18.5 bug where namespace is not used in lookup
  # See: https://github.com/cilium/cilium/issues/XXXXX
  # The CiliumGatewayClassConfig is still created but not referenced

