---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: kagent-openai-es
  namespace: kagent
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: github-secrets
    kind: ClusterSecretStore
  target:
    name: kagent-openai
    creationPolicy: Owner
  data:
    - secretKey: OPENAI_API_KEY
      remoteRef:
        key: KAGENT_OPENAI_API_KEY
